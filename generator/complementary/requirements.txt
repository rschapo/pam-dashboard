# Bases complementares (Agrocore) — Etapas 1 e 2
# Núcleo (Etapa 1)
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0
pyyaml>=6.0
requests>=2.31.0
openpyxl>=3.1.0          # ler planilhas (IBGE Áreas, MapBiomas)
xlrd>=2.0.1              # ler .xls (planilha da IE INCRA nº 5/2022)
pypdf>=4.0.0             # ler o PDF dos Índices Básicos de 2013 do INCRA
tqdm>=4.65.0

# Geoespacial (Etapa 2 — CAR/MapBiomas). Instale quando for tratar geometrias.
geopandas>=0.14.0
shapely>=2.0.0
pyogrio>=0.7.0
pyproj>=3.6.0
# rasterstats>=0.19.0    # só para o piloto CAR×MapBiomas por imóvel (raster)

# Testes
pytest>=7.4.0
